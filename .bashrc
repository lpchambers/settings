# Custom .bashrc
# Lives in ~/.bashrc.d
# User specific aliases and functions
alias vi='vim'
alias sudo='sudo '
alias op='xdg-open'

alias wsync='watch -n 0.5 grep -e Dirty: -e Writeback: /proc/meminfo'

# I CANT TYPE
alias grpe='grep'
alias gre='grep'
alias greo='grep'
alias groe='grep'
